logging:
  level:
    org.springframework: INFO
    ma.gov.pfe: DEBUG

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/users-qua-8888
    username: sa
    password: password
  jpa:
    generate-ddl: true
    show_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
